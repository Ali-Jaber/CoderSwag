<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="166dp">


    <ImageView
        android:id="@+id/categoryImage"
        android:layout_width="@dimen/category_width"
        android:layout_height="@dimen/category_height"
        android:layout_marginStart="@dimen/category_margin_start"
        android:layout_marginTop="@dimen/category_margin_top"
        android:layout_marginEnd="@dimen/category_margin_end"
        android:layout_marginBottom="@dimen/category_margin_bottom"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/hatimage"
        android:contentDescription="@string/category_image" />

    <TextView
        android:id="@+id/categoryName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/white"
        android:textSize="@dimen/category_text_size"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/categoryImage"
        app:layout_constraintEnd_toEndOf="@+id/categoryImage"
        app:layout_constraintStart_toStartOf="@+id/categoryImage"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="HATS" />
</androidx.constraintlayout.widget.ConstraintLayout>